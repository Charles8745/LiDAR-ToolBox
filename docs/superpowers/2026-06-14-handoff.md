# Handoff — 高雄港 LiDAR 數位孿生

- **日期**:2026-06-14(最後更新 **2026-06-18**)
- **狀態**:**F0 / F2 / F4 全部完成、合併並推送**;`main` = `origin/main` 同步(HEAD `71d1787`)。測試 **120 綠**、`tsc` 0、`build` ok。
- **給新 session 的一句話**:在自研 `lidar-engine`(Three.js 點雲引擎)上的高雄港**暗色戰情室數位孿生**已具備:真實佈局 + 真實船舶佔用 + 真實地物(碼頭/防波堤/儲槽3D/起重機3D/錨地)+ NLSC 航照底圖 + 配色階層 + README showcase。**下一階段 = F1 真實 AIS 航跡 _或_ F3 碼頭編號標籤**(二選一;approach 見 2026-06-15 節)。**動工前先讀下方 2026-06-18 節**——尤其「船位是近似、非真實位置」這個 F1 前提。

---

## 🆕 更新 2026-06-18 — F1 真實 AIS 航跡(完成,已於台灣機器目視驗證)

F1「真實 AIS 船位與航跡」**完成並通過瀏覽器目視驗證**(分支 `feat/kaohsiung-port-ais-f1`,測試 **157 綠** / `tsc` 0 / `build` ok;**尚未合併 `main`,待決定 merge/PR**)。探針 + 短窗錄製 + 匯出 + 瀏覽器目視皆已在台灣機器跑過。

- **資料源改用航港局 MPB 公開 AIS GeoJSON**(`https://mpbais.motcmpb.gov.tw/aismpb/tools/geojsonais.ashx`,**免金鑰**,但**需台灣 IP**)。取代先前合成 `BERTH_LINE` 船位。
- **三段解耦管線**:① `record-ais`(standalone 韌性輪詢錄製器,append `.jsonl`)→ ② `export-ais-tracks`(`.jsonl` → per-MMSI tracks JSON,聚合/去重/清洗)→ ③ `main.ts` 依 AIS 時間軸**回放**(插值真實經緯度 + 角度,既有 PointCloud 畫 footprint + 點雲淡尾)。引擎(`src/`)零改動。
- **AIS 接管船位 + 回放時間軸**:船位與時間軸現在由真實 AIS tracks 驅動 —— **修好了「船在陸地上」**(先前船席線性映射的近似位置問題);TWPort 經 IMO→呼號→船名 join 補靜態資料(中文船名/船型/泊位)。
- **KPI 語義改「範圍內 AIS 船數」**(`vesselsInPortAt`,bbox 內即時插值船數),不再是泊位佔用數。
- **新檔**:`data/ais.ts`(純函式:型別 / `parseAisFeature` / `parseAisTime` / bbox / `aggregateTracks` / `cleanTracks` / `mapAisTypeToCategory` / `buildTracksFile`)、`data/probe-ais.ts`(一次性端點探針)、`data/record-ais.ts`(錄製器)、`data/export-ais-tracks.ts`(export CLI)、`time/ais-replay.ts`(`lerpAngleDeg` / `positionAt` / `trailPointsAt` / `vesselsInPortAt` / `incomingAt`)、`data/join.ts`(`joinTwport` / `categoryForTrack`)。測試:`test/port-ais.test.ts` / `test/port-ais-replay.test.ts` / `test/port-join.test.ts`。
- **新 npm scripts**:`port:ais:probe`(探端點、印欄位)/ `port:ais:record`(長跑錄製)/ `port:ais:export`(轉 tracks JSON)。
- **資料替換流程(使用者端)**:24h 錄製器在**另一台台灣機器**上 standalone 長跑(`npm run port:ais:record`)→ `npm run port:ais:export` 產出 `khh-<date>.json` → 把該檔 copy 回本 repo 的 `examples/kaohsiung-port/data/ais-tracks/`,`main.ts` 會自動讀最新一份即可替換資料。raw `.jsonl` 與 `_probe-sample.json` 已 gitignore,**只 commit 短窗 `khh-*.json`**。
- **gitignore**:已加 `examples/kaohsiung-port/data/ais-tracks/raw-khh-*.jsonl` 與 `_probe-sample.json`。

**✅ 目視驗證結果(2026-06-18,台灣機器)**:
- **端點實證**:`geojsonais.ashx` 回 **5616 艘**;真實欄位鍵已對準(`ShipName` / `IMO_Number` / `Call_Sign` / `Ship_and_Cargo_Type` / `Record_Time` 台北時間)。錄了 ~2h 短窗 → `khh-2026-06-18.json`(**257 艘**,已 commit)。
- **船在水面、非陸地**:開場 **227 艘**沿真實港道排列(進港口→各碼頭),坐落水面/碼頭邊 —— **「船在陸地上」確認修好**。
- **真實航跡回放**:領港船 `S3 CHIEN NAN NO 3` 在窗內移動 ~4km、帶 3 點淡出拖尾;scrubber/KPI/趨勢隨時間連動。
- **點船 → join 詳情卡**:點到「立揚 / UNI-PROMOTE」→ 經 IMO join 顯示 全貨櫃船 / S2貨櫃碼頭 / 前後港 / IMO 9202211。
- **篩選**:關掉「工作」可去掉 95 艘漁船/拖船雜訊(227→132)。主控台無 error(僅 favicon 404)。
- **修正(目視時發現)**:原本開場定在 `fromMs`,但錄製窗頭尾因各船軌跡起訖不齊只有 ~1 艘 → 改成**開場掃描挑「在港船數最多」時刻**當預設視角(時間軸範圍仍為完整 from→to)。

**資料特性(給下一步注意)**:此 feed **有 COG 但無 heading**;僅約 **43% 有 IMO**(TWPort join 命中率受限);**漁船/拖船佔比高**(可用「工作」篩選去雜)。

**🔧 目視後 self-review 修正(commit `aa83054`)**:
- **拖尾移除**(使用者決定):船只畫 footprint,不畫點雲淡尾。
- **L2 靠泊船朝向**:無 heading → 靜止船(整段淨位移 <100m)**對齊最近 OSM 碼頭線段**;移動船仍用點間方位角。214/257 判為靠泊。
- **L1 進港改用 TWPort 官方預報**:原 AIS `incomingAt` 在「錄製器已 bbox 預過濾」的資料上語義不成立(退化成「首次出現」、開頭誤判一堆靠泊船)→ 改成右欄清單顯示 **TWPort forecast 真實 ETA/船席/船名**(以快照 `capturedAtMs` 為基準、與 AIS 時鐘解耦);**移除 3D 琥珀標記**(未到港船無誠實位置,且合成船席線會與真實 AIS 位置打架)。`ais-replay` 的 `incomingAt` 一併移除。**注意:committed 快照是 06-14,進港清單為當時預報;要當前預報請重跑 `npm run port:fetch`。**
- **L3 KPI 圓環**:改成「在港 / 當日峰值 %」(原本被夾在 100「艘」誤導)。
- **效能/清理(M1+小)**:per-track 預算快取(類別/join/靠泊/碼頭朝向),消除每幀 2–3 次 join;修過時註解、死參數、snapshot 取最新、詳情卡顯示類別。測試 154 綠。
- **場景尺度 + 去重疊(使用者目視微調,commit `92cb45c`)**:① `WORLD_SCALE` 0.01→**0.025**,且世界單位尺寸改由 `const S = WORLD_SCALE/0.01` 自動等比(LAYERS 地物尺寸 / `SHIP_Y` / footprint 取樣間距皆 `× S`)→ **之後要整體拉大/縮小只改 `WORLD_SCALE` 一個值**;相機距離不再等比跟拉(`radius*1.0+15`),放大才會反映到畫面而非等比抵消。② `SHIP_FOOTPRINT = 0.6`:船畫成真實 LOA 的 60%,讓**相鄰泊位的船留白、不糊成一團**(位置仍真實)。兩者皆 console 可即時感受、留為頂部常數方便再調。

**子專案狀態**:**F1 ✅ 完成 + 目視驗證通過(分支未合併)**;F3(碼頭編號標籤)未開始。

---

## 🆕 更新 2026-06-18 — 配色階層定案 + README showcase + 全部推送 origin

接續 F4 後的收尾,已全部合併 `main` 並**推送 `origin`**(HEAD `71d1787`,`main` = `origin/main` 同步)。

- **配色階層定案**(`palette.ts` `SHIP_CATEGORY_COLORS` + `main.ts` `LAYERS`/bloom)—— 用顏色表達主次,**已定案,別重新討論**:
  - **進港標記** = 紅(警示,bloom 最強);
  - **船** = 均衡 8 類別色(藍貨櫃 / 橙油品 / 棕散雜 / 紫LNG / 粉工作 / 綠軍艦 / 青客運 / 灰其他;Tableau 風,只避開紅);
  - **儲槽 / 起重機** = 中性鋼灰(靠 3D 形狀辨識,不靠色);
  - **海岸線 / 碼頭 / 防波堤 / 錨地** = 暗冷灰退背景(錨地併入結構 bloom 群3);
  - 發光主次:**進港 > 船 > 地標 > 結構**。(過程曾試「船用暖色避藍紅」被否決,最終把藍還給船、地標轉灰。)
- **README showcase**:`README.md` 加「高雄港 LiDAR 戰情室」一節 + hero 截圖 `docs/assets/kaohsiung-warroom.jpeg`(1600×900)。
- **dev guide 修訂**:`docs/vscode-dev-guide.md` 修掉 F4 後過時段(`Y_WATER` 已移除 → 靜態層高度改 `LAYERS.baseY`)、補來源端調整對照與 4 組 bloom 說明、新增 §4h 圖層 registry。
- **品質**:測試 **120 綠**、`tsc --noEmit` 0、`build` 成功、瀏覽器目視驗證。

**⚠️ 給 F1 的重要前提(本 session 已查證)**:**船的位置是近似的,不是真實 / AIS 位置**。`buildShipLayer` → `resolveBerthLatLon` → `berthPositionLatLon` 把船席編號 1–121 線性映射到 `berths.ts` 一條 **8 點手描 `BERTH_LINE`**;實測 21 艘船到最近真實岸線中位 **98m**、最大 **376m**、約 43% 偏 >100m → **這就是「船會在陸地上」的原因**。其餘所有圖層(海岸線/碼頭/防波堤/儲槽/起重機/錨地/底圖)都用真實 OSM/NLSC 經緯度走**同一投影**,確實對齊現實(已逐一驗證)。修正方向 = **F1**(錄製 live AIS → 凍結 snapshot → 24h 回放真實船位),或暫以「snap 到最近 OSM 碼頭幾何」改善 —— 已與使用者確認 **留到 F1 處理**,本 session 不動。

**子專案狀態**:**F0 ✅ / F2 ✅ / F4 ✅(皆已推送 origin);F1(真實 AIS)、F3(碼頭編號標籤)未開始**。

---

## 🆕 更新 2026-06-17 — F4 每類別獨立圖層 + 新增地物(防波堤/儲槽3D/起重機3D/錨地)

靜態地物全面改為**設定驅動的 per-category PointCloud registry**:在 `main.ts` 頂部的 `LAYERS` 陣列(單一事實來源)逐筆描述每個圖層的旋鈕,由 `scene/layers.ts` 的 `buildLayers`/`buildLayerPoints` 工廠建出,舊的合併 `basePC` 已移除。

- **4 個新地物類別**:防波堤(breakwater) 20 條、儲槽 3D 圓柱(storage_tank) 246 個、起重機 3D 龍門(crane) 70 個、錨地圈(anchorage) 5 個,透過擴充後的 `data/fetch-osm.ts` + `parseOsm` 抓取並重烘(`npm run port:osm` 重跑)。
- **3D 點產生器**:`scene/landmarks.ts` 新增三個純函式 `sampleCylinderShell`(儲槽圓柱殼)、`sampleGantry`(龍門起重機骨架)、`sampleZoneRing`(錨地環),皆有單元測試覆蓋。
- **引擎新增**:`PointCloud.setPointSize(px)`(會同步抬高 `uMaxPointSize` 上限,避免超大點被截斷)與 `PointCloud.setBrightness(b)`(`uBrightness` 顏色倍率,預設 1)。
- **Console 即時控制**:每層獨立暴露於 `__twin.layers.<key>`,支援 `.setVisible()` / `.setColor()` / `.setBrightness()` / `.setSize()` / `.setPulseHz()`;六個 key:coastline / pier / breakwater / tank / crane / anchorage。
- **Bloom 群組擴至 4 組**:群組 1=船、2=進港標記、3=結構(coastline/pier/breakwater)、4=地標(tank/crane/anchorage)。
- **品質門檻**:測試 **119 綠**(24 檔)、`tsc --noEmit` 0 錯、`npm run build` 成功、瀏覽器目視驗證通過。
- **已知調校提醒**:錨地圈(`anchorage`)預設較大且亮,可在 Console 執行 `__twin.layers.anchorage.setVisible(false)` 或 `.setBrightness(0.3)` 調暗。

**子專案狀態**:**F0 ✅ / F2 ✅ / F4 ✅;F1(真實 AIS)、F3(碼頭編號標籤)未開始**。

---

## 🆕 更新 2026-06-16 — F0 完成並合併推送(多群組 bloom / 進港閃爍 / 玻璃折射修復)+ VS Code 開發指南

**F0 戰情室視覺基礎全部完成,已合併 `main` 並推送 `origin`**(work HEAD `9e780a7`)。在 F0 基礎上又做了一輪打磨:

- **版面/亮度修復**:左右欄改 flex 直欄(`overlay.ts` `makeRail`,不寫死 `top`,零重疊)、`box-sizing:border-box` 統一寬度、rail `pointer-events:none` 讓空隙點擊穿透(點船正常)。配色定為**中性炭灰**;底圖/玻璃/bloom 壓暗。
- **多群組 selective bloom**:引擎 `bloom` 可吃**陣列**,每組獨立 `strength/radius/threshold`,以圖層編號區分;`addLayer(obj,{bloom:N})` 指派。目前 船=群組1、進港=群組2、海岸線=群組3。(`src/core/postfx.ts` `normalizeBloomGroups` + 每組獨立 composer,mix shader 相加。)
- **點雲閃爍 + 可調色**:`PointCloud` 新增 `pulseHz`(uTime 驅動亮度脈動,連 bloom 一起閃)+ `setPulseHz()`;進港標記用單色 LUT,顏色/頻率是 `main.ts` 頂部常數 `INCOMING_COLOR` / `INCOMING_PULSE_HZ`。
- **⚠️ Liquid Glass 折射重載後不顯影 — 已修**:Chromium 對 SVG `backdrop-filter` 的 `feImage`(canvas→PNG data-URI)**非同步解碼**,合成節點不等解碼完重建 → 面板只剩平面磨砂,要一次**樣式表層級重算**才現(所以「存 theme.css 就出現、存 .ts 又消失」;改 inline / CSS 變數沒用)。修法 `ui/overlay.ts` `reviveGlass()`:attach 後頭幾秒多次「拆 backdrop-filter → 還原 → clone+cache-bust 換同源 `<link>`」(`[400,900,1800,3500]ms`)。手動觸發 `window.__reviveGlass()`。完整原理寫進 **`~/Desktop/UI-ToolBox/CLAUDE.md`「已知陷阱」一節(該 repo 尚未 commit)**。
- **VS Code 開發指南**:`.vscode/`(建議擴充、vite 背景任務、Chrome/Edge launch)+ `docs/vscode-dev-guide.md`(專案地圖、調校配方、bloom/pulse/glass、`__twin` 把手清單、資料管線、疑難排解,含截圖)。

**測試 100→103 綠**。**注意**:`main.ts`/`theme.css` 留有**進行中的視覺數值**(進港 `[205,38,38]`、`pulseHz 0.8`、三組 bloom 參數、`--lg-tint …0.1`、fog/底圖),下一階段可續調。glass revive 在載入頭幾秒有約一影格的拆除閃爍(robust 取捨,可後續收斂成單次)。

**子專案狀態**:**F0 ✅ / F2 ✅ 完成合併**。**下一階段 = F1 真實 AIS 航跡、F3 碼頭編號標籤**(approach 見下方 2026-06-15 節)。

---

## 🆕 更新 2026-06-15 — 「3D UI 戰情室」軌跡啟動,F2 完成

§8 的方向已收斂成一條「類數位孿生 3D UI / 戰情室」軌跡,拆成 **4 條獨立子專案**(各自 spec→plan→實作)。其餘報告模組(碳權/RAG/氣象/PPO)暫不碰。

- **F2 衛星/航照底圖 ✅ 完成、合併 `main`、推送 `origin`**(HEAD `a373a46`)。離線海圖 plane → 真實 **NLSC PHOTO2 正射航照**(免金鑰,`wmts.nlsc.gov.tw`,EPSG:3857);`npm run port:basemap` 烘焙成單張 `examples/kaohsiung-port/data/basemap-khh.jpg`+`.json`,執行期材質染暗(`0x3a5a72`,`__twin.setBasemapTint` 可調)。新檔:`geo/tiles.ts`(純數學+測試)、`data/fetch-basemap.ts`。底圖預設**開**。測試現 **93 綠**(原 87 + 6 tile)。需 `sharp`(devDependency)重烘。spec/plan:[specs/2026-06-15-kaohsiung-port-nlsc-basemap-design.md](specs/2026-06-15-kaohsiung-port-nlsc-basemap-design.md)、[plans/2026-06-15-kaohsiung-port-nlsc-basemap.md](plans/2026-06-15-kaohsiung-port-nlsc-basemap.md)。
- **F0 戰情室視覺基礎 ✅ 完成、已合併 `main` 並推送**(後續打磨見上方 2026-06-16 節):① 引擎新增**可選 post-processing**(預設關,洞穴 demo 不受影響)—— `src/core/postfx.ts` selective bloom(只 `shipPC`+`incPC` 發光;bloom pass 清成純黑避免泛白)+ 點 shader 支援 three 內建 fog(`fog:true` + 合併 `UniformsLib.fog`)。`LidarEngine` 新增 `bloom`/`fog` options 與 `addLayer(obj,{bloom:true})`。② HUD 改用**桌面 UI-ToolBox 的 Liquid Glass Kit**(vendored 進 `examples/kaohsiung-port/ui/liquid-glass.{css,js}`),`overlay.ts` 重寫為**指揮中心版面 B**(頂 navbar/時鐘、左 stat+環形儀表+船型篩選、右 24h 趨勢折線+即將進港清單、底時間軸),介面契約不變 + 新增 `setTrend`/`setIncoming`。③ 配色**中性炭灰(Ink Wash)** `#0B0C0E`/`#20242B`/銀 `#CBD5DF`/警示 `#FF8A4D`(`ui/theme.css`);底圖 re-tint `0x2a2e33`。趨勢/進港皆**真實**(`buildOccupancyTrend`/`buildIncomingList`)。測試 **100 綠**。瀏覽器目視驗證通過(發光/玻璃/fog/scrub)。spec/plan:[specs/2026-06-15-kaohsiung-port-warroom-f0-design.md](specs/2026-06-15-kaohsiung-port-warroom-f0-design.md)、[plans/2026-06-15-kaohsiung-port-warroom-f0.md](plans/2026-06-15-kaohsiung-port-warroom-f0.md)。
- **F1 真實 AIS 航跡**(未開始):aisstream **無歷史 API** 且瀏覽器藏不住金鑰 → **錄製 live AIS→凍結 snapshot→24h 回放**(複用 TWPort snapshot 模式,正面回應 §6「不做假動畫」);InstancedMesh + 淡出拖尾。
- **F3 碼頭編號標籤**(未開始):**troika-three-text** SDF + 距離 LOD + raycaster hover(別用 CSS2D:會卡、無法被遮擋)。

研究(2026-06-15,web + 兩個政府站)結論:海委會 `ocean.moi.gov.tw/3DMap` = **ArcGIS 4.x + NLSC WMTS**(日間 GIS);高港 `sdci.twport.com.tw/khbweb/osmx2.aspx` = **Leaflet 2D 船席圖**(船名»船公司 + 作業註記 + 碼頭編號)。兩者皆非暗色戰情室大屏 → 這是我們的差異點。

---

## 1. 現在的狀態(可直接驗證)

```bash
npm test     # 19 檔 87 測試全綠
npm run build # vite 打包 + tsc 宣告,成功
npx tsc --noEmit -p tsconfig.json  # 全專案型別檢查 0 錯
npm run dev   # 開瀏覽器 → /examples/kaohsiung-port/index.html
```

- `main` HEAD = `b01f9d4`,已 push 到 `origin/main`(GitHub: Charles8745/LiDAR-ToolBox)。
- 開發分支 `feat/kaohsiung-port-twin` 已合併並刪除(linear / fast-forward,33 個 commit)。
- 工作樹乾淨。視覺驗證截圖在 `.playwright-mcp/`、設計 mockup 在 `.superpowers/brainstorm/`(皆 gitignore)。

## 2. 文件入口

- **設計 spec**:[specs/2026-06-14-kaohsiung-port-lidar-twin-design.md](specs/2026-06-14-kaohsiung-port-lidar-twin-design.md)(6 大決策、資料來源、誠實邊界、階段切分)
- **實作計畫**:[plans/2026-06-14-kaohsiung-port-lidar-twin.md](plans/2026-06-14-kaohsiung-port-lidar-twin.md)(17 個 TDD 任務)
- **引擎原始設計**:[specs/2026-06-13-lidar-scan-engine-design.md](specs/2026-06-13-lidar-scan-engine-design.md)

## 3. 六大已定決策(沿用,別重新討論)

1. 互動 = **常駐點雲**(港口一直可見,顏色直接表達狀態,非掃描揭示)
2. 資料 = **真實佈局 + 真實船舶**(OSM + 港務公司 TWPort 開放資料)
3. 視角 = **可環繞 orbit**
4. 時間 = **即時快照 + 完整 24h 時間軸沙盤**
5. 技術 = **自研引擎當渲染器 + 地理真實資料管線**(借「真實性」但不用 deck.gl)
6. 背景 = **B 海岸線點雲(預設)+ C 地圖底圖(可切換)**

## 4. 架構速覽

**引擎(`src/`)— 全部是加法擴充,洞穴 demo (`examples/basic`) 與舊 API 不受影響**
- `PointCloud`:新增 options `colorMode:'distance'|'value'`(預設 distance)、`maxPointSize`、`sizeAttenuation`(預設 true);方法 `setColorMode()`、`addPoints(positions, values, time?)`;`readonly valueArray`。
- `LidarEngine`:新增 options `cameraMode:'lookAround'|'orbit'`(預設 lookAround)、`cameraPosition/cameraTarget/cameraFar`、`autoScan`(預設 true,false 時跳過掃描迴圈當「渲染宿主」)、`colorMode/maxPointSize/sizeAttenuation`;方法 `addLayer(obj3d)`、getter `camera3D`;`scannable`/`emitter` 變為 optional。orbit 用 three 內建 `OrbitControls`(已在 vite.config 外部化 `^three\/examples\/`)。
- `ramps`:新增 `buildCategoryLUT(colors)`(NearestFilter 類別色,無混色)。
- `src/index.ts` 新增匯出:`PointCloud`、`PointCloudOptions`、`buildRampTexture`、`buildRampTextureFromFn`、`buildCategoryLUT`。
- 著色:shader 用 `mix(vDist01, vValue01, step(0.5, uColorMode))`;value 模式下 `aValue=(類別index+0.5)/類別數`(配 NearestFilter LUT)。

**高雄港 app(`examples/kaohsiung-port/`)— 純邏輯模組皆有 node 單元測試**
| 檔案 | 職責 |
|---|---|
| `geo/projection.ts` | `createProjection`、`KAOHSIUNG_ORIGIN`(22.59,120.30)、`WORLD_SCALE`(0.01 = 1 單位/100m);經緯度→世界座標(北=-z、東=+x) |
| `data/twport.ts` | `parseTwportXml`、`parseTaipeiDate`(UTC+8)、`parseBerthNo`(`#108碼頭`→108)、`VesselRecord` |
| `data/fetch-snapshot.ts` | `npm run port:fetch` → `data/snapshots/khh-YYYY-MM-DD.json` `{capturedAtMs, berthing, forecast}` |
| `data/osm.ts` | `parseOsmWays`、`OverpassDoc/OverpassEl/LatLon/Polyline/OsmGeometry` |
| `data/fetch-osm.ts` | `npm run port:osm` → `data/osm-khh.json` `{coastline, piers}` |
| `berths.ts` | `BERTH_LINE`(東岸碼頭線 waypoints)、`MIN_BERTH=1`/`MAX_BERTH=121`、`sampleAlong`、`berthPositionLatLon`、`resolveBerthLatLon` |
| `time/occupancy.ts` | `buildIntervals`、`occupancyAt`、`berthStatusAt`、`BerthInterval/BerthStatus` |
| `palette.ts` | `SHIP_CATEGORIES`(8 類)、`SHIP_CATEGORY_COLORS`、`shipCategoryIndex`、`STATUS_CATEGORIES/COLORS`、`statusIndex`、`BASE_COLORS`、`valueFor` |
| `scene/portPoints.ts` | `samplePolyline`、`sampleShipFootprint`、`buildBaseLayer`、`buildShipLayer`(回傳含 `centers`)、`TYPE_DIMS_M`(依 ShipCategory keyed) |
| `ui/overlay.ts` | `createOverlay`(KPI/圖例/篩選/檢視切換/詳情卡/時間軸)、`fmtClock` |
| `main.ts` | 組裝:兩層點雲 + 進港標記層 + orbit + 疊層 + 點擊挑選 + 自動取景 + 地圖底圖平面 |
| `index.html` | 殼層 |

**已實作功能**:常駐點雲、依船型上色、orbit、自動取景、KPI、船型篩選、船型↔狀態檢視切換、點船看詳情卡、24h 時間軸(拖曳+播放)、即將進港琥珀標記、B/C 底圖切換(C 由 OSM 即時繪成海圖式地圖平面、與點雲對齊、零外部依賴)。

## 5. 真實資料(2026-06-14 快照)

- **TWPort 開放資料**(免金鑰):`https://tpnet.twport.com.tw/IFAWeb/Reports/OpenData/GetOpenData?port=KHH&type=N`(BIG5 XML)。type=1 當日指泊(67 艘,40 在編號碼頭)、type=5 進港預報(53 艘)。
- **OSM Overpass**(免金鑰):`man_made=pier` + `natural=coastline`(75 海岸線 + 88 碼頭線)。
- 資料每天會變;app 讀**凍結快照**(可重現)。要更新:`npm run port:fetch` / `npm run port:osm`。

## 6. 誠實邊界 / 已知小問題(別當成 bug 重修,除非要正式化)

- **是「泊位佔用隨時間」,不是連續 AIS 航跡**。船在佔用區間內出現於泊位、區間外消失;沒有逐秒移動。→ 這就是 Task 17「進港滑入動畫」被**刻意延後**的原因(做假動畫會誤導資料)。要真航跡需接 aisstream(免費需金鑰)。
- 碼頭座標是**描圖內插的近似**(順序對、絕對位置非測量級)。
- `MAX_BERTH=121`:資料中有 #122、#145(會被夾到南端;`rebuildIncoming` 只掃 1–121,這兩個的進港標記會略過)。改 `MAX_BERTH` 會位移所有泊位(別亂改),要修就讓 `rebuildIncoming` 改吃資料實際出現的 berthNo。
- 約 12 筆資料 `leaveMs < actPortMs`(髒資料),被 `Math.max(endMs, startMs+1)` 壓成 1ms 區間 → 那些船幾乎不出現。
- `occupancyAt` 重疊時「後者勝」依賴陣列順序(`[...berthing, ...forecast]` → 預報勝),非時間排序。規模小無妨。
- app 層 window 事件監聽無 teardown(單頁 example,無 dispose 路徑)。

## 7. 開發/驗證小抄

- 瀏覽器主控台無 error;`window.__twin` 暴露 `{ engine, shipPC, incPC, mapPlane, layers, updateShips, updateIncoming, refresh, play, pause, fromMs, toMs, nowMs, tracks, shipCenters }` 供除錯(F4 起 `basePC` 由 `layers.<key>` 取代;見 2026-06-17 節)。**F1 起把手更名/增刪**:`rebuildShips`/`rebuildIncoming` → `updateShips`/`updateIncoming`,新增 `play()`/`pause()`(自走回放);`intervals` 已移除(改由 AIS `tracks` + `fromMs`/`toMs` 時間軸驅動)。
- 視覺驗證流程:`npm run dev` 背景啟動 → 用瀏覽器工具導到 `/examples/kaohsiung-port/index.html` → 截圖。點大小在 orbit 距離會掉到 1px,所以 app 層點雲都用 `sizeAttenuation:false` + 明確 pointSize。

## 8. 下一步候選(待新 session 討論、各自立 spec)

- **進港滑入動畫**(Phase 2 eye-candy)或**真實 AIS 航跡**(aisstream)。
- **碼頭編號標籤**(hover/縮放顯示)。
- **線上衛星/Mapbox 高解析底圖**(取代 C 的離線海圖)。
- 報告其他模組各自立 spec:碳權代幣化 PoC、LLM+RAG 政策報告、ConvLSTM 短時氣象、疫情航跡追溯(可重用 `VesselRecord.beforePort/nextPort`)、PPO 多目標排程引擎。

---

**接手方式**:`npm run dev` 看現況 → 讀本檔 §4/§6 → 決定要推進 §8 哪塊 → 用 `/superpowers:brainstorming` 開該子題的 spec。
